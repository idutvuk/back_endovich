[project]
name = "006-sql-playground"
version = "0.1.0"
description = "SQL playground: many-to-many + ручка для прямого SQL"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115",
    "sqlalchemy>=2.0",
]
